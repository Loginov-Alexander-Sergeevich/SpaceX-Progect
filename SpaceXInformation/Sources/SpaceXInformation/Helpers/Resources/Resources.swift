import UIKit

enum Resources {
    enum Images {
        enum BackgroundImage {
            static var rocketImage: UIImage { UIImage(named: "BackgroundPic", in: .module, with: nil)! }
        }
    }
}
