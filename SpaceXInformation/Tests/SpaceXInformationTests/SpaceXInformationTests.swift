import XCTest
@testable import SpaceXInformation

final class SpaceXInformationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SpaceXInformation().text, "Hello, World!")
    }
}
